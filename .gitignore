# 构建产物：exe 由 build-single-exe.ps1 生成，不入库
dist/

# 离线安装包：体积大且各人不同，只保留说明
offline/*
!offline/README.txt

# Python 编译缓存
__pycache__/
*.pyc

# 编辑器 / 工具
.claude/
.vscode/
*.swp

# 测试临时产物（由 tests/*.ps1 运行时生成，不入库）
tests/.unattended-results.txt
tests/.log-*.txt
tests/.diag*.txt
tests/.unattended-debug.ps1
